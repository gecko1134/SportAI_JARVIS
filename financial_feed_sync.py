
import streamlit as st

def run():
    st.title("💸 Financial Feed Sync")

    st.markdown("All modules now flow into pro forma logic:")
    st.success("✅ Events → Revenue")
    st.success("✅ Sponsorships → Tiered income")
    st.success("✅ Staffing → Expense log")
    st.warning("⚠️ Add expense categories for non-sports events in next update.")
